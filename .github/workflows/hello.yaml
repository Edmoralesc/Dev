name: hello-world
on:
  push:
    branches: ["stage"]

env: 
  TEXT: "Edwin"

jobs:
  prints:
    runs-on: ubuntu-latest

    steps:
      - name: print-to-screen
        run: echo "hello world!! ${{TEXT}}"

      - name: print-to-screen
        run: echo "secretvalue!! ${{secrets.SECRET_VALUE}}"